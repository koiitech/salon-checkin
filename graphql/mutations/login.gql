mutation LoginEmployee($email: String!, $password: String!) {
  loginEmployee(email: $email, password: $password) {
    token {
      access_token
    }
    user {
      id
      name
      email
    }
  }
}
